import React, { Component } from "react";
import { connect } from "react-redux";

class ProductListGalleryContainer extends Component {
  render() {
    return <div></div>;
  }
}

export default connect()(ProductListGalleryContainer);
